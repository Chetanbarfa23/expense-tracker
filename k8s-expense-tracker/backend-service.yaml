apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-service

spec:
  type: NodePort

  selector:
    app: expense-tracker

  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001
